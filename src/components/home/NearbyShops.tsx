import SectionTitle from "../common/sectionTitle/SectionTitle";
import styles from "./NearbyShops.module.css";

const NearbyShops = () => {
    return (
        <div className={styles.section}>
            <SectionTitle>우리 동네 인기 헤어샵</SectionTitle>
        </div>
    );
};

export default NearbyShops;
